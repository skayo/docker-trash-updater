version: 2

updates:
  # Enable version updates for GitHub Action workflows
  - package-ecosystem: github-actions

    # Use root directory, because dependabot expects the workflows at .github/workflows
    directory: /

    # Check daily at 11 o'clock
    schedule:
      interval: daily
      time: '11:00'
      timezone: Europe/Vienna
